import { ViweDirective } from './viwe.directive';

describe('ViweDirective', () => {
  it('should create an instance', () => {
    const directive = new ViweDirective();
    expect(directive).toBeTruthy();
  });
});
